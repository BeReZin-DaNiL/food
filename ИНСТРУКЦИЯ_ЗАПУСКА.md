# 📋 Инструкция по запуску сайта FoodDelivery

Этот проект полностью настроен для легкого запуска. Вам не нужно настраивать сложные базы данных — всё работает "из коробки".

## 🚀 Как запустить сайт

### Вариант 1: Через XAMPP (рекомендуется)
1. Скопируйте папку с проектом `food` в папку `htdocs` вашего XAMPP (обычно `C:\xampp\htdocs\food`).
2. Запустите **XAMPP Control Panel**.
3. Нажмите **Start** напротив **Apache**. (MySQL запускать НЕ обязательно, так как мы используем встроенную базу данных).
4. Откройте браузер и перейдите по ссылке:
   👉 **[http://localhost/food/](http://localhost/food/)**

### Вариант 2: Через встроенный PHP сервер
Если у вас установлен PHP, можно запустить без XAMPP:
1. Откройте папку проекта в консоли (командной строке).
2. Введите команду:
   ```bash
   php -S localhost:8000
   ```
3. Откройте в браузере: **[http://localhost:8000](http://localhost:8000)**

---

## ⚙️ Первичная настройка (База данных)

При первом запуске вам нужно создать базу данных. Мы сделали это автоматически.

1. Перейдите на страницу установки:
   👉 **[http://localhost/food/install.php](http://localhost/food/install.php)** (или `http://localhost:8000/install.php`)
2. Вы увидите процесс создания таблиц. Если всё зеленое — отлично!
3. Нажмите кнопку **"Перейти на главную"**.

> **Важно:** База данных хранится в файле `database.sqlite` внутри папки проекта. Не удаляйте этот файл, если не хотите потерять всех пользователей и заказы.

---

## 🗄️ Как посмотреть базу данных?

Мы создали специальную страницу для просмотра содержимого базы данных (пользователи, заказы, товары).

👉 **[Открыть просмотр базы данных](http://localhost/food/admin_viewer.php)**
(или `http://localhost:8000/admin_viewer.php`)

Там вы увидите:
- **users** — Зарегистрированные пользователи
- **products** — Товары в меню
- **orders** — История заказов

---

## 📱 Функционал сайта
1. **Регистрация/Вход:** Работает, данные сохраняются в базу.
2. **Профиль:** Отображает данные текущего пользователя.
3. **Меню:** Товары загружаются из базы данных.
4. **Корзина:** Можно добавлять товары (работает через сессии).

## ❓ Решение проблем
* **Ошибка "Port 3306 blocked"**: Игнорируйте. Наш сайт теперь работает на SQLite и не зависит от порта 3306.
* **Белый экран**: Проверьте, что запускаете файл через сервер (`http://localhost...`), а не просто открываете файл `index.php` как картинку.
